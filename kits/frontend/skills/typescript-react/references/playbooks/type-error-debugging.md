# Type Error Debugging

This page is kept as a compatibility entry point for older links.

The canonical incident-debugging playbook lives in:

- [frontend-debugging/references/type-error-debugging.md](../../../frontend-debugging/references/type-error-debugging.md)

Keep type-design and prevention guidance in `typescript-react`; keep step-by-step incident debugging in `frontend-debugging` so the two skills do not drift.
